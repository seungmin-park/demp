spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/demp?serverTimezone=UTC&characterEncoding=UTF-8
    username: inhatc
    password: inhatc
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
#        format_sql: true
#        use_sql_comments: true

  jwt:
    secret: pepega524832857240

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  #logging.level:
#  org.hibernate.SQL: debug
  # org.hibernate.type: trace

file:
  dir: C:/repo/dempfrontend/src/assets/

server:
  port: ${port:8080}

cloud:
  aws:
    credentials:
      access-key: AKIAYGX4NA2A6S6PAWVY
      secret-key: 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
    region:
      static: ap-northeast-2
    s3:
      bucket: inhatc-demp
    stack:
      auto: false